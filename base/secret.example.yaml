apiVersion: v1
kind: Secret
metadata:
  name: ai-platform-secrets
type: Opaque
stringData:
  # Do NOT commit real secrets. Create the real Secret in your cluster, or use SealedSecrets/ExternalSecrets.
  JWT_SECRET: change-me

