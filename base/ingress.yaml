apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-task-processing-platform-ingress
  namespace: ai-task-processing-platform
spec:
  ingressClassName: traefik
  rules:
    - host: ai-task-processing-platform.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 5000
